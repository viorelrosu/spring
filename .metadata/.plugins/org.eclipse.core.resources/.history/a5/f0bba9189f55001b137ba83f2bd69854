package t7.ej07;

import java.io.IOException;
import java.util.HashMap;
import java.util.Map;

import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

@WebServlet("/Ej07")
public class Ej07 extends HttpServlet {
	private static final long serialVersionUID = 1L;
       
	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		Map<String, String> colores = new HashMap<>();
		colores.put("azul","Azul");
		colores.put("rojo","Rojo");
		colores.put("amarillo","Amarillo");
		colores.put("verde","verde");
		
		String coloresInputs = generaCheckbox("colores", colores);
		
		response.getWriter().println(coloresInputs);
		
	}

	private String generaCheckbox(String nombre, Map<String, String> colores) {
		String res = "";
		for( String k: colores.keySet() ) {
			res += "<input type=\"checkbox\" name=\""+nombre+"[]\" value=\""+colores.get(k)+"\" id=\""+colores.get(k)+"\" /><label for=\""+colores.get(k)+"\">"+k+"</label><br />";
		}
		return res;
	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		doGet(request, response);
	}

}
